<div class="form-check form-switch">
    <input class="form-check-input" type="checkbox" id="{{ $prefixId }}{{ $name }}" name="{{ $name }}">
    <label class="form-check-label" for="{{ $name }}">{{ __($label) }}</label>
</div>
